services:
  - type: web
    name: anoma-intent-demo
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -b 0.0.0.0:$PORT src.web:app
    envVars:
      - key: HOST
        value: 0.0.0.0

